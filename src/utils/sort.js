import _ from "lodash";

export function sort(items, value) {
  return items;
}

// handleSort = () => {
//   let movies = this.state.movies.sort((a, b) =>
//     a.value > b.value ? 1 : b.value > a.value ? -1 : 0
//   );
//   console.log(movies);
//   this.setState({ movies });
// };
